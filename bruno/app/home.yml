info:
  name: home
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/home"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
