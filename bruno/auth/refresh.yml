info:
  name: refresh
  type: http
  seq: 3

http:
  method: POST
  url: {{baseUrl}}/v1/auth/refresh
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
