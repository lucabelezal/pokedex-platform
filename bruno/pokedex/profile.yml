info:
  name: profile
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/v1/me"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
