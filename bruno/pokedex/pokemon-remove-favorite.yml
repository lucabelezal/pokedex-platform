info:
  name: pokemon-remove-favorite
  type: http
  seq: 13

http:
  method: DELETE
  url: "{{baseUrl}}/v1/pokemons/25/favorite"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
