info:
  name: home-filter-region
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/v1/home?region=kanto"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
