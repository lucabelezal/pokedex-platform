info:
  name: home-ordering
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/v1/home?order=Maior%20número"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
