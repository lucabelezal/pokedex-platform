info:
  name: favorites
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/v1/me/favorites"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
