info:
  name: pokemon-list
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/v1/pokemons?page=0&size=20"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
