info:
  name: pokemon-add-favorite
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/v1/pokemons/25/favorite"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
