info:
  name: regions
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/v1/regions"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
