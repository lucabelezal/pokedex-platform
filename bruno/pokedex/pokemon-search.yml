info:
  name: pokemon-search
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/v1/pokemons/search?q=pikachu&page=0&size=20"
  headers:
    - name: Authorization
      value: "Bearer {{authToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
