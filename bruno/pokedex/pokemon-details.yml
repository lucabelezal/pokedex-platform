info:
  name: pokemon-details
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/v1/pokemons/25/details"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
