name: CI Go (Build, Test, Lint)

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]
  push:
    branches:
      - main
      - develop

jobs:
  db-migrations:
    name: DB Migrations
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: pokedex_ci_db
          POSTGRES_USER: pokedex
          POSTGRES_PASSWORD: pokedex
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U pokedex -d pokedex_ci_db"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Instalar cliente PostgreSQL
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      - name: Aplicar migration incremental
        env:
          DATABASE_URL: postgres://pokedex:pokedex@localhost:5432/pokedex_ci_db?sslmode=disable
        run: ./core/infra/postgres/migrations/apply.sh

  go-ci:
    name: Go CI - ${{ matrix.module }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - module: core/app/pokemon-catalog-service
            artifact_name: coverage-app-pokemon-catalog-service
          - module: core/bff/mobile-bff
            artifact_name: coverage-bff-mobile-bff
          - module: core/infra/postgres/json2sql
            artifact_name: coverage-infra-postgres-json2sql

    defaults:
      run:
        working-directory: ${{ matrix.module }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: ${{ matrix.module }}/go.mod
          cache: true
          cache-dependency-path: ${{ matrix.module }}/go.mod

      - name: Go mod tidy check
        shell: bash
        run: |
          go mod tidy
          git diff --exit-code -- go.mod
          if [[ -f go.sum ]]; then
            git diff --exit-code -- go.sum
          fi

      - name: Go fmt check
        shell: bash
        run: |
          fmt_out="$(gofmt -l .)"
          if [[ -n "$fmt_out" ]]; then
            echo "Arquivos fora do gofmt:" 
            echo "$fmt_out"
            exit 1
          fi

      - name: Go vet
        run: go vet ./...

      - name: Build
        run: go build ./...

      - name: Test
        run: go test -race -covermode=atomic -coverprofile=coverage.out ./...

      - name: Upload cobertura
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.module }}/coverage.out
          if-no-files-found: ignore

  golangci-lint:
    name: GolangCI-Lint
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module:
          - core/app/pokemon-catalog-service
          - core/bff/mobile-bff
          - core/infra/postgres/json2sql

    defaults:
      run:
        working-directory: ${{ matrix.module }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: ${{ matrix.module }}/go.mod
          cache: true
          cache-dependency-path: ${{ matrix.module }}/go.mod

      - name: Instalar golangci-lint
        shell: bash
        run: go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest

      - name: Run golangci-lint
        shell: bash
        run: |
          "$(go env GOPATH)/bin/golangci-lint" run --timeout=5m
